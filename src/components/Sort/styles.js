import styled from 'styled-components';

export const SortDropdown = styled.select`
    padding: 10px 15px;
    border: none;
    border-radius: 8px;
    background-color: #e0e0e0;
    margin-left: 8px;
    font-size: 1rem;
    cursor: pointer;
    outline: none;
`;